"""
used to represent a group of objs as a single unit. where,
1. insertion order preserved
2. duplicates are allowed
3. are mutable

* index starts from 0, with this duplicate elements can b differentiated. py supports both positive n negative indexing
*  not hashable (bcoz list is mutable)
* diff ways to create a list:::
   1. empty list
   2. list with data
   3. list from keyboard === using eval() function
   4. list by using list() type converter
   5. list using split() function


"""